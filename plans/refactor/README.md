# Refactor Plans — maintainability & future-proofing series

Generated by the improve skill on 2026-07-11, against commit `f95f3b5` **plus
the large uncommitted working-tree changes present at that date** — all code
excerpts in these plans were verified against the working tree, not the
commit. Numbering continues from the earlier advisor series in `plans/`
(001–013).

Maintainer's brief for this series: the codebase grew complex after the
security refactoring (provider microservice, bigger Next.js app, huge shadcn
components); make it maintainable, less cluttered, future-proof, easily
self-hostable, and reliable. **Excluded by instruction: test coverage and
security auditing.**

**Verification convention (unchanged from the 001-series):** no automated
tests are to be added. Every change is verified with `bun run typecheck`,
`bun run lint`, and `bun run build` (the only gate that catches
cacheComponents/Suspense errors); registry changes additionally run
`bun run registry:build`. The maintainer does manual UI/behavior testing.

**Working-tree warning for executors:** the tree contains ~128 uncommitted
modified files unrelated to any single plan. Stage only your plan's in-scope
files; never `git add -A`.

Plan selection note: the maintainer requested a comprehensive refactor plan
set (not a top-3); all ten high-leverage findings groups below were planned.
This selection was made autonomously per the maintainer's standing brief.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 014 | Delete 44 unused UI components, dead files, 6 unused deps | P1 | S | LOW | — | TODO |
| 022 | Self-host ops hardening (auto-migrate, health, internal port, docs) | P1 | S | LOW | — | TODO |
| 020 | Provider-service hygiene (dead code, boot validation, log context, Vault timeout) | P1 | M | LOW | — | TODO |
| 017 | Deduplicate /api/v1 route plumbing (cursor, credit errors, sole-product) | P1 | M | MED | — | TODO |
| 016 | Consolidate copy-to-clipboard primitives | P2 | S | LOW | — | TODO |
| 015 | Split 1132-line products-section.tsx into products/ module | P2 | M | MED | after 014 (soft) | TODO |
| 018 | Split 859-line credits.ts into credits/ package (move-only) | P2 | M | MED | after 017 | TODO |
| 021 | Shared secret-encryption module; seed-script crypto diet | P2 | M | MED | after 020 | TODO |
| 019 | Restructure registry browser client + shared formatting | P2 | M | MED | — | TODO |
| 023 | Gate marketing surface off self-hosted instances | P3 | M | MED | maintainer sign-off on the decision section | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale).

## Recommended approach

1. **Quick wins first (014, 022, 020):** independent, low-risk, each verified
   by tsc/build/lint. 014 alone removes ~45 files and 6 dependencies.
2. **Then the dedup/structure pair (017 → 018):** 017 extracts shared API
   helpers; 018 then splits credits.ts move-only. Sequential because both
   orbit `@/lib/credits` imports.
3. **UI structure (016, 015):** small primitive consolidation, then the big
   component split. 015 supersedes old plan 010 and its stale branch — do not
   merge `advisor/010-split-products-section-component`.
4. **Cross-package (021, 019):** 021 after 020 (same secrets file). 019
   touches only registry/.
5. **023 last:** it encodes a product decision (marketing only on the hosted
   instance, keyed off `PAY_BILLING_MODE`); maintainer should confirm the
   Decision section before an executor runs it.

Plans touching disjoint files (e.g. 014+022+020) can run in parallel
worktrees.

## Dependency notes

- **018 after 017**: 017 adds `src/lib/api/credit-errors.ts` importing from
  `@/lib/credits`; landing 018's path-stable split second avoids branch churn.
- **021 after 020**: both edit `services/provider/src/secrets/index.ts`.
- **015 after 014 (soft)**: 014 deletes unused ui components; doing it first
  keeps 015's import surface clean. No hard conflict.
- **010 (old series) is SUPERSEDED by 015.**

## Findings considered and rejected (so they aren't re-audited)

- **Split `src/lib/api/http.ts` into auth/cors/rate-limit modules**: rejected —
  280 lines of small focused functions with clear names; splitting adds
  imports without adding clarity. Revisit only if it passes ~450 lines.
- **Generic wallet-resolution / route-handler factory across code+external
  routes**: rejected as over-abstraction — the differences (auth kind, mode
  check, invalid-code rate limiting) are the point of the two route families.
  017 extracts only the identical fragments.
- **Wallet-kind polymorphism (`WalletResolver` interface)**: rejected —
  speculative abstraction for a 2-variant system with no third variant planned.
- **Decompose `fulfillment.ts` into phases**: rejected — 456 lines, cohesive,
  transaction-critical; the risk outweighs the readability gain.
- **Outbound webhooks are "fire-and-forget without retry"** (audit claim):
  **corrected** — `deliverProjectEvent` IS awaited in the webhook route; a
  failure marks the event `error` and returns 500, so the provider retries.
  Residual nuance: a retry after successful fulfillment but failed delivery
  re-runs fulfillment's idempotent path; a durable delivery queue is recorded
  as a direction option below, not a defect.
- **registry.json missing `error.ts` in component items** (audit claim):
  **rejected/false** — components get it transitively via
  `registryDependencies` → pay-provider → pay-client.
- **FormDialog remount-on-open loses half-typed state**: rejected — the
  `key={String(open)}` remount is an intentional action-state reset; losing
  drafts on close is standard dialog behavior.
- **Add memoization across dashboard components**: rejected — no observed
  problem; premature at current list sizes.
- **ConfirmAction vs MutationForm consolidation**: rejected — two patterns
  with a real semantic difference (destructive-confirm vs inline mutation).
- **Provider-service FK missing on `owner_id`** in its schema copy: noted, not
  planned — the web app owns migrations; the service schema is deliberately a
  read-subset. A comment in the service schema would suffice; fold into any
  future schema-touching change.
- **CHANGELOG.md**: process suggestion, maintainer's call; not planned.
- **Structured logger / request IDs across services**: reduced to op-context
  logging in 020; full structured logging deferred until multi-instance
  debugging demands it.
- **Registry `formatCredits` locale prop**: deferred — browser-locale default
  is acceptable; documented in 019's maintenance notes.
- **Pagination for `GET /api/v1/products`** and prior 001-series rejections
  (CORS preflight `*`, seed password, monthly-grant timezone, cnfast): still
  rejected — see `plans/README.md`.

## Direction options (maintainer's call, not planned as work)

- **Durable outbound-webhook delivery queue**: today consumer-endpoint
  delivery rides on provider retry semantics. A `outbound_webhook_deliveries`
  table with backoff + a dashboard replay view would decouple delivery from
  provider retries. Meaningful build; wait for real demand.
- **Generate registry client types from the API** (OpenAPI → `lib/pay` types):
  kills silent client/server type drift while honoring the zero-dependency
  rule (codegen at build time, not runtime deps). Spike-sized.
- **`hosted` billing mode consolidation**: plan 023 gives `PAY_BILLING_MODE` a
  second job (marketing gate). If hosted billing itself stays lightly used,
  consider documenting it as the "official instance" switch generally.
